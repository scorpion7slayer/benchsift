# Artificial Analysis API - https://artificialanalysis.ai
# The code tries each key in order until one succeeds. Fill in as many as you
# have; empty values are skipped.
ARTIFICIAL_ANALYSIS_API_KEY=
ARTIFICIAL_ANALYSIS_FALLBACK_API_KEY=
ARTIFICIAL_ANALYSIS_FALLBACK_API_KEY_2=
ARTIFICIAL_ANALYSIS_FALLBACK_API_KEY_3=
ARTIFICIAL_ANALYSIS_FALLBACK_API_KEY_4=

# OpenRouter API - https://openrouter.ai
# Optional, but recommended for authenticated /api/v1/models metadata requests.
OPENROUTER_API_KEY=

# Hugging Face Hub API - https://huggingface.co/settings/tokens
# Optional. Used for richer model metadata: license, downloads, gated/private
# state and Hub links. `HUGGING_FACE_API_KEY` is also accepted.
HUGGINGFACE_API_KEY=

# Shared secret for the manual refresh endpoint (/api/cron/refresh).
CRON_SECRET=

# Optional path for the persisted models cache. In Dokploy, mount a volume at
# /app/.data or set this to a mounted path if you want cache persistence across
# redeploys.
MODELS_CACHE_FILE=.data/models-cache.json
